language: python
python:
    - "2.6"
    - "2.7"
    - "3.3"
    - "3.4"
    - "pypy"

install:
    - pip install -q -r requirements.txt --use-mirrors

script:
    - ./tests/manage.py test Artifactorial
