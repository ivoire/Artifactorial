language: python
python:
    - "2.7"
    - "3.2"
    - "3.3"
    - "3.4"
    - "pypy"

install:
    - pip install -r requirements.txt
    - pip install coveralls

script:
    - coverage run --branch --source=Artifactorial tests/manage.py test Artifactorial

after_success:
    coveralls
